class User {
  String username;
  String password;
  String firstname;
  String lastname;
  User.register(this.username, this.password, this.firstname, this.lastname);
  User(this.username, this.password);
}
