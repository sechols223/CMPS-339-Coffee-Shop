{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "proxt": "http://localhost:3000",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "build": "npm run server",
    "heroku-postbuild": "npm install && npm run build",
    "server": "npm install && nodemon"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "nodemon": "^2.0.16",
    "path": "^0.12.7"
  }
}
